spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eda-akkaya/microservices-1.git
          default-label: main
          search-paths:
            - configurations
            - configurations/{application}
          clone-on-start: true
          force-pull: true
server:
  port: 8989
